#include "core/dims.h"

namespace llm {

// Определение вне класса нужно в C++14, ровно как у StrView::npos: без него
// использование kMaxRank по ссылке или по адресу не собирается. Попадает
// туда оно незаметно — LLM_CHECK_MSG выводит константу в поток, а потоковый
// оператор берёт её по константной ссылке.
//
// При -O3 компилятор подставляет значение и ошибки не видно; поймать её можно
// сборкой без встраивания, и тогда получается «undefined reference to
// llm::Dims::kMaxRank».
const int Dims::kMaxRank;

}  // namespace llm
