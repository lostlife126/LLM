build/
build-*/
*.o
*.a
compile_commands.json
.cache/
.vscode/
# Со слешем в начале — только каталог корпуса и чекпоинтов в корне проекта.
# Без него правило поглотило бы и src/data/ с исходниками датасета.
/data/
/checkpoints/
